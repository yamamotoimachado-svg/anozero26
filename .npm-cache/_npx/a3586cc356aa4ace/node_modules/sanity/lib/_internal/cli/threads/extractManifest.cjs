"use strict";
var node_worker_threads = require("node:worker_threads"), _internal = require("@sanity/schema/_internal"), resolveIcon = require("../../../_chunks-cjs/resolveIcon.cjs"), getStudioWorkspaces = require("../../../_chunks-cjs/getStudioWorkspaces.cjs"), mockBrowserEnvironment = require("../../../_chunks-cjs/mockBrowserEnvironment.cjs");
async function main() {
  if (node_worker_threads.isMainThread || !node_worker_threads.parentPort)
    throw new Error("This module must be run as a worker thread");
  const opts = node_worker_threads.workerData;
  if (!isExtractManifestWorkerData(opts))
    throw new Error('Invalid worker data: expected an object with a "workDir" string property');
  const iconResolver = opts.resolveIcons === !1 ? void 0 : (props) => resolveIcon.resolveIcon({
    ...props,
    sanitize: !0
  }), cleanup = mockBrowserEnvironment.mockBrowserEnvironment(opts.workDir);
  try {
    const workspaces = await getStudioWorkspaces.getStudioWorkspaces({
      basePath: opts.workDir
    }), pending = [];
    for (const workspace of workspaces)
      pending.push(_internal.extractCreateWorkspaceManifest(workspace, iconResolver).then((manifest) => node_worker_threads.parentPort?.postMessage(manifest)));
    await Promise.all(pending);
  } finally {
    node_worker_threads.parentPort?.close(), cleanup();
  }
}
function isExtractManifestWorkerData(data) {
  return typeof data == "object" && data !== null && "workDir" in data && typeof data.workDir == "string";
}
main().then(() => process.exit());
//# sourceMappingURL=extractManifest.cjs.map
